# frontend/ — review interface

Added in **Stage 7**. Next.js + TypeScript review UI: the document queue, the
source page image with bounding-box overlays, field-level correction with an
append-only history, and the approval action that locks a final snapshot. A `web`
service will be added to `docker-compose.yml` at that point.
